{
  "name": "matrix.nullstar.fun",
  "version": "1.0.0",
  "description": "the operating system of AI",
  "type": "module",
  "scripts": {
    "lint": "eslint \"**/*.js\" --max-warnings 0",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "prepare": "husky"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "^1.0.0",
    "eslint": "^8.57.0",
    "husky": "^9.1.7",
    "jsdom": "^23.0.0",
    "vitest": "^1.0.0"
  }
}
